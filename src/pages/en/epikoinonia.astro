---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageStub from '../../components/PageStub.astro';
import { useTranslations, type Lang } from '../../i18n/utils';

const lang = (Astro.currentLocale ?? 'en') as Lang;
const t = useTranslations(lang);
---

<BaseLayout title={t('page.epikoinonia.title') + ' - INNOLAND'} description={t('page.epikoinonia.intro')}>
  <PageStub titleKey="page.epikoinonia.title" introKey="page.epikoinonia.intro" />
</BaseLayout>
