---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageStub from '../components/PageStub.astro';
import { useTranslations, type Lang } from '../i18n/utils';

const lang = (Astro.currentLocale ?? 'el') as Lang;
const t = useTranslations(lang);
---

<BaseLayout title={t('page.erga.title') + ' - INNOLAND'} description={t('page.erga.intro')}>
  <PageStub titleKey="page.erga.title" introKey="page.erga.intro" />
</BaseLayout>
